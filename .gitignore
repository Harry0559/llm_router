node_modules/

# Next.js
web/.next/
web/out/
web/tsconfig.tsbuildinfo

# Local env (API URLs, secrets)
.env
.env.local
.env.*.local

# Proxy SQLite (runtime traces)
proxy/data/*.db
proxy/data/*.db-shm
proxy/data/*.db-wal

# Local Claude Code / agent config (do not commit)
.claude/

.DS_Store
Thumbs.db
CLAUDE.md
